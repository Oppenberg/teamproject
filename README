install Sysbench and MySQL
1.	sudo apt-get install mysql-server
2.	sudo apt install sysbench

to run 10 instances of mysql sysbench benchmark locally:
./run10.sh

To run just one instance of the local benchmark:
1. prepare the database
   sysbench /usr/share/sysbench/oltp_read_write.lua --mysql-db=mysql --mysql-host=localhost 
   --mysql-user=root --mysql-password=supervisor --mysql-port=3306 --debug=on --db-driver=mysql prepare
2. run the test
   sysbench /usr/share/sysbench/oltp_read_write.lua --mysql-db=mysql --mysql-host=localhost 
   --mysql-user=root --mysql-password=supervisor --mysql-port=3306 --debug=on --db-driver=mysql run   

to run the Dockerfile: 
Docker run as905993/mysql

alternatively: 
Docker run <image name>

Note: the above benchmark is running the oltp_read_write test
