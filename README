to run 10 instances of mysql sysbench benchmark locally:
./run10.sh

the above benchmark is running the oltp_read_write test

-----old instructions----------------------------------------------------------------------
to prepare the database:
sysbench --oltp_read_write --oltp-table-size=2000000 --mysql-db=test --mysql-host=arldcn24 --mysql-user=admin --mysql-port=3306 --mysql-password=supervisor prepare

It assumes that mysql is installed and running at port 3306.
and the password is supervisor.
if you install the mysql using apt-get, thats the default password.
The user is 'root' instead of admin
and you should execute the command:
mysql -uroot -psupervisor -h127.0.0.1 -P3306
CREATE DATABASE test;
For sysbench to run.
